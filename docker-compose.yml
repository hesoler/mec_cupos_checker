services:
  app:
    build: .
    container_name: mec_checker
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data/available.json:/app/data/available.json
      - ./data/storage_state.json:/app/data/storage_state.json
