services:
  app:
    build: .
    env_file:
      - .env
    volumes:
      - ./state.json:/app/state.json
      - ./storage_state.json:/app/storage_state.json
      - ./mec_cupos_checker.log:/var/log/mec_cupos_checker.log
